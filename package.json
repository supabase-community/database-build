{
  "name": "@database.build/root",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "scripts": {
    "dev": "turbo watch dev",
    "build": "turbo run build"
  },
  "workspaces": ["apps/*", "packages/*"],
  "devDependencies": {
    "supabase": "^2.0.0",
    "turbo": "^2.3.2"
  }
}
